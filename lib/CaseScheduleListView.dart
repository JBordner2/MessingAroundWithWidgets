import 'package:flutter/material.dart';

class CaseScheduleListView extends StatelessWidget {
  const CaseScheduleListView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
        home: SafeArea(
            top: true,
            child: Scaffold(
              body: Text("Jack Border"),
            )
        )
    );
  }
}
